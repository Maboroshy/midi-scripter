# Start DAW by MIDI Controller

This script starts DAW (or any other app) when a set MIDI message received.

## Prerequisites

- 'To DAW' virtual port for output.
- Replace `'MIDI Controller'` with your MIDI controller port name.
